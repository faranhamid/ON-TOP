# ON TOP Production Environment Configuration
# This file contains production-ready environment variables
# Copy this to .env and update with your actual production values

# --- Server Configuration ---
PORT=3002
NODE_ENV=production
JWT_SECRET=ontop_production_jwt_secret_2024_change_this_to_secure_random_string

# --- Database Configuration ---
# For production, use PostgreSQL/Supabase instead of SQLite
DATABASE_PROVIDER=pg
DB_HOST=your-project.supabase.co
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=your_secure_database_password
DB_PORT=5432
DB_SSL=true

# --- Supabase Configuration (for file storage and backups) ---
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key
SUPABASE_UPLOADS_BUCKET=ontop-uploads
SUPABASE_BACKUPS_BUCKET=ontop-backups

# --- Payment Processing (Optional - App runs without payments) ---
# STRIPE_SECRET_KEY=sk_live_your_stripe_live_secret_key
# STRIPE_WEBHOOK_SECRET=whsec_your_stripe_webhook_secret
# APPLE_SHARED_SECRET=your_apple_iap_shared_secret
# APPLE_VERIFY_ENV=production

# --- AI Services ---
OPENAI_API_KEY=sk-your_openai_api_key_here
OPENAI_MODEL=gpt-4o-mini

# --- Production URLs ---
# Update these with your actual production domain
PRODUCTION_DOMAIN=https://your-domain.com
API_BASE_URL=https://api.your-domain.com

# --- Security ---
# Generate a strong random string for JWT secret
# You can use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=generate_a_secure_random_string_here

# --- Monitoring & Analytics (Optional) ---
# SENTRY_DSN=your_sentry_dsn_for_error_tracking
# GOOGLE_ANALYTICS_ID=your_ga_tracking_id

# --- Email Service (Optional, for notifications) ---
# SENDGRID_API_KEY=your_sendgrid_api_key
# FROM_EMAIL=noreply@your-domain.com
