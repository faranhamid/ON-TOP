# Vercel Environment Variables Template
# Copy these variables to your Vercel project settings

# === REQUIRED VARIABLES ===

NODE_ENV=production

# Generate a secure JWT secret (run: openssl rand -hex 64)
JWT_SECRET=your_secure_jwt_secret_here

# Database Configuration (PostgreSQL recommended)
DATABASE_PROVIDER=pg
DB_HOST=your-database-host.com
DB_NAME=your_database_name
DB_USER=your_database_user
DB_PASSWORD=your_database_password
DB_PORT=5432
DB_SSL=true

# OpenAI API Key (required for Emma AI)
OPENAI_API_KEY=sk-your_openai_api_key_here

# === OPTIONAL VARIABLES ===

# AI Model (default: gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini

# iOS In-App Purchase (optional)
APPLE_SHARED_SECRET=your_apple_iap_shared_secret
APPLE_VERIFY_ENV=production

# Supabase (for file storage, optional)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Rate Limiting (optional overrides)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100
RATE_LIMIT_AUTH_WINDOW_MS=900000
RATE_LIMIT_AUTH_MAX=10
